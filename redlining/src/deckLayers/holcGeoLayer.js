import React from 'react';
import DeckGL, {GeoJsonLayer} from 'deck.gl';

export function renderHolcOverlay(param) {
    const { mapViewState } = param.props;
    const { width, height } = param.state;
}

function _renderDotsLayer(param) {
}